# Prompt — P02 secure booking

Resume from checkpoint. Confirm P00/P01 dependencies are done.
Implement the active P02 task only. The server must derive price/status/time,
verify Turnstile, enforce idempotency and preserve legacy admin compatibility.
Run negative contract tests and checkpoint with evidence.
