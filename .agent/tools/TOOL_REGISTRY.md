# Tool Registry

Allowed by default: repository read/search, non-destructive shell, test/build, local static analysis.

Explicit approval required: database mutations, RLS changes, production deploy, secret rotation, destructive Git commands, force push, deleting customer data.

Unavailable tool behavior: mark task blocked and provide exact manual step; never fabricate execution.
