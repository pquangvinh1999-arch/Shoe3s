# Prompt — P03 frontend foundation

Resume from checkpoint. Build the no-WebGL booking path first. Preserve
`/?page=order` and `/`. Split booking/admin bundles. Do not add production 3D
assets until the shell passes booking E2E, keyboard, mobile and reduced-motion.
