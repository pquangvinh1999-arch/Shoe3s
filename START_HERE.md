# Wed3s Work + Codex Orchestrator v2

1. Copy all files into the root of `pquangvinh1999-arch/Wed3s` without deleting the existing `.agents/` AG Kit.
2. Keep both systems:
   - Existing `.agents/`: legacy AG Kit knowledge.
   - New `.agent/`: project-specific canonical orchestration.
3. Run `bash scripts/preflight.sh` or `powershell -File scripts/preflight.ps1`.
4. Start a **new Work chat** with `WORK_START.md` for Steps 1–4.
5. Handoff through Git using `SWITCH_WORK_CODEX.md`.
6. Use Codex for implementation/testing; use new sessions for Step 8 and independent reviews.
7. Do not deploy before Step 13 passes.
